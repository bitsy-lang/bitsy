enum type State
    @Idle
    @Running of Tuple<Word<4>, Bit>
end

pub mod Top
    incoming in  : Word<8>@d
    outgoing out : Word<8>@d

    pub wire io.out <= { io.in + 1 }

    pub wire io.out <= { tuple(1, 2, 3) }
end
